# CS215-hw-reading-txt-data-files
Used the .read(), .readlines() and .readline() methods and played around with how to output number of characters, lines and words in a txt file with the different methods. 
